# 開発作業
## リストのソート
現状だとリストの順番が変更できない。
金額順で並べ替えられた方が良いのでソート機能（手動）を追加する。

## 全体設定の追加
トリガー設定と同じ様なウィンドウで
UIマネージャーとデータクラスは新規に作成する。
設定項目は環境設定.mdを参照。

## 入力欄の挙動がおかしい
入力欄が一般的なモノと違い、入力中の文字が入力欄に表示されないという問題がある

## データセーブ、ロードロジックの分離
データのセーブロードはSettingData.csで行われているので十分分離できている気はするが一応確認する。


# 設定画面のコード直書きをやめる
UIToolkitでデザインしたUIをEventSettingUIManagerから操作できるようにしたい。

# 設定画面をそもそも別ウィンドウに分離する
Unityだとむりっぽい

# それ以外
## 使い方を書く

## コメントの付与
生成したコードはコメントが不足しているのでコメントをつけていく

## ドキュメントの修正
概要.mdでデータのセーブロードについて記載する。

## 仕様書フォルダ作成
仕様についてはdocument内にspecificationフォルダを作りそこへ格納する。
README.mdの内容はOverview.mdを作りそちらへ移動。Overview.md内から各種仕様書へのリンクを張る。